# Open Graph images

Default share image: `streamplay4k-default.jpg`, 1200×630, referenced by
`src/_data/seo.js`. Pages and duplicate/campaign pages can override it with an
`ogImage` value; blog posts fall back to their `featuredImage`.
